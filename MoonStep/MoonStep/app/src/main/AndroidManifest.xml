<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.administrator.moonstep">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/logo_compress"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/logo_compress"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        android:largeHeap="true">
        <activity android:name=".guide_activity.GuidePage"
            android:theme="@style/Theme.AppCompat.DayNight.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".login_activity.Login_activity"/>
    </application>

</manifest>